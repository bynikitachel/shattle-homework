let everyThing = document.querySelectorAll('*');
for (var i = 0; i < everyThing.length; i++) {
    // everyThing[i] = String(everyThing[i])
    if (i === everyThing.length - 1) { console.log(everyThing); }
}